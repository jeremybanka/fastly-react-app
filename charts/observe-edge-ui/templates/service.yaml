apiVersion: v1
kind: Service
metadata:
  name: {{ include "observe-edge-ui.fullname" . }}
  labels:
    {{- include "observe-edge-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.internalPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "observe-edge-ui.selectorLabels" . | nindent 4 }}
